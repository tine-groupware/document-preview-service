stages:
 - build
 - test
 - deploy
 - clean
 
cache:
 key: build
 paths:
  - build

build:
 stage: build
 artifacts:
  paths:
   - documentPreview.tar.gz
 script:
  - php psh.phar build

test:
 stage: test
 script:
  - php psh.phar test
  
deploy:
 stage: deploy
 script:
 - echo 1
  
clean:
 stage: clean
 script:
  - php psh.phar clear
  