build ``docker build . -t document-preview``

start with ``docker-compose up``

shell with ``docker exec -it document-preview /bin/bash``

port: 5000

xdebug:
port: 9001
IDEkey: serverName=documentPreview
ip: 172.18.0.1

todo:
ssl

